name: LlamaCon99
options:
  bundleIdPrefix: com
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true
  developmentLanguage: es

targets:
  LlamaCon99:
    type: application
    platform: iOS
    sources:
      - LlamaCon99
      - Shared
    info:
      path: LlamaCon99/Info.plist
      properties:
        CFBundleDisplayName: Llama Con 99
        CFBundleShortVersionString: 1.0.1
        CFBundleVersion: "1"
        UILaunchScreen: {}
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
        LSApplicationQueriesSchemes:
          - tel
          - telprompt
        NSContactsUsageDescription: Para mostrar tus contactos y llamarlos directamente desde la app.
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.llamacon99
        MARKETING_VERSION: 1.0.1
        CURRENT_PROJECT_VERSION: 1
        SWIFT_VERSION: "5.9"
        TARGETED_DEVICE_FAMILY: "1"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
    entitlements:
      path: LlamaCon99/LlamaCon99.entitlements
      properties:
        com.apple.security.application-groups:
          - group.com.llamacon99.shared
    dependencies:
      - target: CallerIDExtension
        embed: true
    scheme:
      testTargets: []

  CallerIDExtension:
    type: app-extension
    platform: iOS
    sources:
      - CallerIDExtension
      - Shared
    info:
      path: CallerIDExtension/Info.plist
      properties:
        CFBundleDisplayName: CallerID
        CFBundleShortVersionString: 1.0.1
        CFBundleVersion: "1"
        NSExtension:
          NSExtensionPointIdentifier: com.apple.callkit.call-directory
          NSExtensionPrincipalClass: $(PRODUCT_MODULE_NAME).CallDirectoryHandler
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.llamacon99.CallerIDExtension
        MARKETING_VERSION: 1.0.1
        CURRENT_PROJECT_VERSION: 1
        SWIFT_VERSION: "5.9"
        TARGETED_DEVICE_FAMILY: "1"
        SKIP_INSTALL: NO
    entitlements:
      path: CallerIDExtension/CallerIDExtension.entitlements
      properties:
        com.apple.security.application-groups:
          - group.com.llamacon99.shared
